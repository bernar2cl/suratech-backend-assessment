apiVersion: v1
kind: Service
metadata:
  name: quote-service
  labels:
    app: quote-service
spec:
  type: ClusterIP
  selector:
    app: quote-service
  ports:
    - name: http
      port: 80
      targetPort: 8080